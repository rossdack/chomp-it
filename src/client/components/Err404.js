import React, {Component} from 'react';

export default class App extends Component {
    render() {
        return (
            <span>That URL was not found. Shorten a new URL <a href="/">here</a>.</span>
        );
    }
}